#include "main.h"
#include <math.h>

/**
* _sqrt_recursion - prints the aquare root of natural number)
* @x: base interger
* @y: raised power interger
* Return: Returns value
*/
int _sqrt_recursion(int n)
{
	if (n < 0)
		return (-1);
	if (n > 1)
		return (_sqrt_recursion(n));
		{
			if (_sqrt_recursion(n) = float)
			return (-1);
		}
	return (_sqrt_recursion(n));
}
